// Runtime config for the ABX page. The Pages workflow overwrites this at deploy
// time with the real endpoint/token from secrets and the codec build version.
window.ABX_CONFIG = {
  endpoint: "",
  token: "",
  version: "",
};
