// Runtime config from abx_config.js (a classic script setting window.ABX_CONFIG).
// Fields: endpoint, token, version. All empty on a plain local checkout.
export const config = window.ABX_CONFIG || {};
